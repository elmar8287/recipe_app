<div class="">
  <div class="">
    <%= link_to recipe.name, recipe_path(recipe.id) %>
    <%= button_to "REMOVE", recipe_path(recipe.id), data: { confirm: 'Are you sure you want to delete this recipe?' }, method: :delete %>
  </div>
  <div class="recipe-secondary">
    <p><%= recipe.description %></p>
  </div>
</div>
